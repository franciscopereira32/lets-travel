extends layout 

block content 
    h2=title 
        .country_wrapper 
            ul 
                each country in allCountries 
                    li 
                        a(href=`/countries/${country}`) #{country}
                else
                    li There are no countries